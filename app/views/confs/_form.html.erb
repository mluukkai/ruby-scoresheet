<%= form_for(@conf) do |f| %>
  <% if @conf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conf.errors.count, "error") %> prohibited this conf from being saved:</h2>

      <ul>
      <% @conf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exercise_count %><br>
    <%= f.number_field :exercise_count %>
  </div>
  <div class="field">
    <%= f.label :repository_base %><br>
    <%= f.text_field :repository_base %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
